[project]
name = "mxu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cocotb>=2.0.1",
    "cocotb-test>=0.2.6",
    "numpy>=2.4.2",
    "pytest>=9.0.2",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
